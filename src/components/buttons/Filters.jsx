import React from "react";

const Filters = ({ Icon }) => {
    return (
        <button className="p-1.5 rounded-md border-2 border-gray">
            <Icon />
        </button>
    );  
}

export default Filters;
